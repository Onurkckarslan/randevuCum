services:
  - type: web
    name: randevucum-app
    env: python
    plan: standard

    buildCommand: |
      pip install -r requirements.txt && python migrate_pg.py

    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT

    envVars:
      - key: PYTHON_VERSION
        value: 3.11
