services:
  - type: web
    name: randevucum
    env: python
    plan: free

    buildCommand: pip install -r requirements.txt
    startCommand: python run.py

    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: PORT
        value: 8000
